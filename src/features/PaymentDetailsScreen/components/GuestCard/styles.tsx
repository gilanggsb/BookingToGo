import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    marginVertical: 12,
    borderWidth: 1,
    borderRadius: 6,
    padding: 10,
    flexDirection: 'row',
    gap: 14,
    overflow: 'hidden',
    alignItems: 'center',
  },
});
