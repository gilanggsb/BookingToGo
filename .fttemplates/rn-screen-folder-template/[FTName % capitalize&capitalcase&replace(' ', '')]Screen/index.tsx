import React from 'react';
import {ScrollView} from 'react-native';
import styles from './styles';
import use[FTName | capitalize&capitalcase&replace(' ', '')] from "./use[FTName | capitalize&capitalcase&replace(' ', '')]";
import {MainView} from '@components/MainView';
import {MainText} from '@components/MainText';





const [FTName | capitalize&capitalcase&replace(' ', '')]Screen = () => {
  const {themeColors} = use[FTName | capitalize&capitalcase&replace(' ', '')]();
  return (
    <MainView backgroundColor={themeColors.screen.background} flex={1}>
    <ScrollView>
      <MainText>[FTName | capitalize&capitalcase&replace(' ', '')]Screen</MainText>
    </ScrollView>
  </MainView>
  );
};

export {[FTName | capitalize&capitalcase&replace(' ', '')]Screen};
